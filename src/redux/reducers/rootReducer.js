import routeCalculatorReducer from './routeCalculatorReducers'
import {
    combineReducers
} from 'redux';

const rootReducer = combineReducers({
    routeCalculatorReducer,
})

export default rootReducer;
